# packages-another-module

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test packages-another-module` to execute the unit tests via [Jest](https://jestjs.io).
